% Generated by roxygen2: do not edit by hand
\name{clear_cache}
\alias{clear_cache}
\title{Clear Cached Data}
\description{
Clears cached data files. Can remove all files or only files older than a specified period.
}
\usage{
clear_cache(older_than = NULL)
}
\arguments{
\item{older_than}{Optional. Remove only files older than this period (e.g., "7 days"). If NULL, removes all files.}
}
\value{
Invisible NULL. Prints number of files removed to console.
}
\examples{
\dontrun{
# Clear all cached data
clear_cache()

# Clear only files older than 7 days
clear_cache(older_than = "7 days")
}
}
\keyword{utilities}
